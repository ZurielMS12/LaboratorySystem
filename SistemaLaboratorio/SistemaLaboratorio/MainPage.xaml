<Page
    x:Class="SistemaLaboratorio.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="containerMain" Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="65" />
            <RowDefinition Height="*" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Border Background="Aqua" CornerRadius="0,10,10,0">
            <TextBlock Text="AQUI VA A ESTAR UN LOGO" Foreground="Black" />
        </Border>
        <Border Background="Beige" Grid.Column="1">
            <TextBlock Text="AQUI OTROS ELEMENTOS VISUALES" Foreground="Black" />
        </Border>

        <SplitView x:Name="split" Grid.Row="1" Grid.RowSpan="2" Grid.ColumnSpan="2" Background="LightBlue" IsPaneOpen="True" PanePlacement="Left" OpenPaneLength="250" CompactPaneLength="48" DisplayMode="CompactInline" >
            <SplitView.Pane>
                <Grid x:Name="menuGrid" />
            </SplitView.Pane>
            <SplitView.Content>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height=".02*"/>
                        <RowDefinition/>
                        <RowDefinition Height=".02*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width=".01*"/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width=".01*"/>
                    </Grid.ColumnDefinitions>
                    <Grid x:Name="BorderAllowFrame" Grid.Row="1" Grid.Column="1" CornerRadius="10" Background="Transparent">
                        <!--<Frame x:Name="ContentFrame" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>-->
                    </Grid>
                </Grid>
            </SplitView.Content>
        </SplitView>
    </Grid>
</Page>